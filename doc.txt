git branch
 git status
 git add .
  git commit -m "msg"
  git push


//Register
POST: 127.0.0.1:3100/register
firstname:Required
lastname:Optional
email:uniqe
password:Required



//LOGIN
POST:  127.0.0.1:3100/login

ENTER :emailID & password :show a token

if case user account soft deleted display message: User account deleted


//update

PUT:  127.0.0.1:3100/profile




>> service control helper
with validation



///--------------------
USer details table 


//multerServices
  multerServices(req, res) {
   
    try {
      let resPayload = {
        message: MESSAGES.IMAGE_UPLOADED,
        // payload: user,
      };
      Response.success(res, resPayload);
      //res.send(req.files);
    } catch (error) {
      let resPayload = {
        message: "",
        // payload: user,
      };
      Response.error(res, resPayload);
      // res.json({
      //   error,
      // });
    }
  }